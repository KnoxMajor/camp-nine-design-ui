import React, { useState } from 'react';

const ComponentSpace = ({ component }) => {
  return (
    <div style={{minHeight: '300px', width: '100px'}}>
      {component}
    </div>
  )
}

export { OneTimeCode };
